CREATE TABLE IF NOT EXISTS users(
id INTEGER NOT NULL,
age integer,
email varchar,
name varchar,
primary key (id)
)
CREATE TABLE USERS(
   ID INTEGER PRIMARY KEY     NOT NULL,
   NAME           TEXT    NOT NULL,
   EMAIL           TEXT    NOT NULL,
   AGE            INT     NOT NULL
);